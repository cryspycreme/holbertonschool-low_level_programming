#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * @argc: no. of arguments passed
 * @argv: the strings of arguments passed into function
 * Return: always 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
