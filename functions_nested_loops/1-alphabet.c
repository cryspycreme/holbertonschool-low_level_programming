#include "main.h"

/**
 * main -Entry Point
 *
 * Return: always 0 (Success)
 */

void print_alphabet(void)
{
	char ch = 97;

	while (ch <= 122)
	{
		_putchar(ch);
		ch++;
	}
	_putchar ('\n');
}

int main(void)
{
	int print_alphabet(void);
	return (0);
}
